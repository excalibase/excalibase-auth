apiVersion: v1
kind: Service
metadata:
  name: {{ include "excalibase-auth.fullname" . }}
  labels:
    {{- include "excalibase-auth.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "excalibase-auth.selectorLabels" . | nindent 4 }}
